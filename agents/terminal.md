Проверка активности: Если терминальная команда выполняется слишком долго без вывода данных, агент должен имитировать нажатие клавиши Enter (отправить символ новой строки \n), так как процесс может ожидать подтверждения.Обновление статуса: Агент обязан предоставлять промежуточные обновления статуса во время выполнения длительных операций.Обработка ошибок: В случае отсутствия ответа от терминала агент должен выполнить проверку на наличие скрытых ошибок выполнения, вместо того чтобы сообщать, что команда «никогда не вернулась».Приоритет вывода: Избегайте запуска команд исключительно в фоновом режиме, если пользователю требуется видеть текущий прогресс в терминале.Восстановление после зависания: Если команда кажется зависшей, агент должен попытаться прервать её и перезапустить, либо явно сообщить пользователю о задержке, чтобы тот мог вмешаться вручную.
Check for errors on command execution if this happens
Give it status updates
